# Closed-Loop Case Study: Evasion Discovery and Retraining

*(Note: Fill out this template after running the full end-to-end pipeline with the raw Kaggle datasets and Anthropic API key enabled).*

## 1. The Baseline Defense
We trained our primary XGBoost classifier on Attack 1 (Phishing) and Attack 2 (Vishing) synthetic data, alongside the standard Sparkov fraud dataset. 
- **Baseline Precision**: [X.XX]
- **Baseline Recall**: [X.XX] (At 1% FPR)

## 2. The Generative Attack (The Gap)
We then unleashed our `Attack 3 - Camouflage` adversarial generator against the baseline. The LLM was instructed to break down $15,000 fraudulent transfers into smaller chunks to evade detection, simulating an adaptive adversary.
- **Evaded Transactions**: The baseline classifier, lacking sequence velocity features, failed to flag [XX]% of the camouflaged sequences, treating the smaller transactions as legitimate behavior.

## 3. Closing the Loop
We took the successful camouflaged sequences generated by the LLM and fed them back into the `data/synthetic/attack_3/` directory for retraining.
We updated `detectors/features/pipeline.py` to extract `tx_count_24h` and `tx_amount_24h`.

## 4. The Hardened Defense
After retraining the classifier on the new, harder adversarial dataset:
- **New Precision**: [X.XX]
- **New Recall**: [X.XX] (At 1% FPR)

**Conclusion**: By generating the attack we feared, we discovered a gap in our static rules and patched it before it could hit a production system. This is the value of the AI Defense Lab.
